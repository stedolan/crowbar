(jbuild_version 1)

(library
 ((name crowbar)
  (public_name crowbar)
  (c_names (instrumentation_stubs))
  (ocamlopt_flags (:standard -afl-inst-ratio 0))
  (modules (:standard \ (cb2)))
  (libraries (cmdliner ocplib-endian afl-persistent str psq yojson))))
